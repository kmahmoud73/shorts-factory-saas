# System
.DS_Store

# Leads — private
leads.json
sent_log.json

# Internal docs — NOT for GitHub Pages
multi_channel_strategy.html
onboarding_internal.html

# Secrets
.env
*.secret
*.pem
*.key

# Private architecture/strategy doc — never commit to public repo
CLAUDE.md
SAAS_CLAUDE.md
.site_stats_last.json
web_leads.jsonl
.wrangler/

# Lead PII — never publish (public repo)
reengagement_log.jsonl
.reengagement_gate_state.json
.reply_cache.json
LEAD_AUDIT_*.md
lead_audit_*.md
*_leads.jsonl
